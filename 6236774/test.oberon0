MODULE Test;

CONST
  INPUTLENGTH = 5;

VAR
  i: INTEGER;
  a: ARRAY INPUTLENGTH OF INTEGER;
  r: RECORD x:INTEGER; y:INTEGER END;

BEGIN
  i := 0;
  WHILE i < INPUTLENGTH DO
    Read(a[i]);
    Write(a[i]+1); 
    WriteLn;
    i := i + 1
  END

END Test.