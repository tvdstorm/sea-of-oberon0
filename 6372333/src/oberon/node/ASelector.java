/* This file was generated by SableCC (http://www.sablecc.org/). */

package oberon.node;

import java.util.*;
import oberon.analysis.*;

@SuppressWarnings("nls")
public final class ASelector extends PSelector
{
    private final LinkedList<PRealselector> _realselector_ = new LinkedList<PRealselector>();

    public ASelector()
    {
        // Constructor
    }

    public ASelector(
        @SuppressWarnings("hiding") List<PRealselector> _realselector_)
    {
        // Constructor
        setRealselector(_realselector_);

    }

    @Override
    public Object clone()
    {
        return new ASelector(
            cloneList(this._realselector_));
    }

    public void apply(Switch sw)
    {
        ((Analysis) sw).caseASelector(this);
    }

    public LinkedList<PRealselector> getRealselector()
    {
        return this._realselector_;
    }

    public void setRealselector(List<PRealselector> list)
    {
        this._realselector_.clear();
        this._realselector_.addAll(list);
        for(PRealselector e : list)
        {
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._realselector_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._realselector_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        for(ListIterator<PRealselector> i = this._realselector_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PRealselector) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
