/* This file was generated by SableCC (http://www.sablecc.org/). */

package oberon.node;

import java.util.*;
import oberon.analysis.*;

@SuppressWarnings("nls")
public final class ARecordType extends PType
{
    private final LinkedList<PFieldlst> _fieldlst_ = new LinkedList<PFieldlst>();

    public ARecordType()
    {
        // Constructor
    }

    public ARecordType(
        @SuppressWarnings("hiding") List<PFieldlst> _fieldlst_)
    {
        // Constructor
        setFieldlst(_fieldlst_);

    }

    @Override
    public Object clone()
    {
        return new ARecordType(
            cloneList(this._fieldlst_));
    }

    public void apply(Switch sw)
    {
        ((Analysis) sw).caseARecordType(this);
    }

    public LinkedList<PFieldlst> getFieldlst()
    {
        return this._fieldlst_;
    }

    public void setFieldlst(List<PFieldlst> list)
    {
        this._fieldlst_.clear();
        this._fieldlst_.addAll(list);
        for(PFieldlst e : list)
        {
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._fieldlst_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._fieldlst_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        for(ListIterator<PFieldlst> i = this._fieldlst_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PFieldlst) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
