MODULE testmodule;

CONST
  MAXLENGTH	= 5;
  
VAR
  array: ARRAY MAXLENGTH OF INTEGER; 
  i,j,k: INTEGER;

PROCEDURE testproc(VAR xx, yy: INTEGER; plus: INTEGER);

 BEGIN
	 xx := xx + plus;
	 yy := yy + plus;
END testproc;

BEGIN
 i := 1;
 j := 2;
 k := 6;
 testproc(i, j, MAXLENGTH)
END testmodule.