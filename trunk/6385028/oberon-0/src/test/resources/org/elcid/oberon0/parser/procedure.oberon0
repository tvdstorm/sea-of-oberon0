MODULE Const;

VAR
  a: INTEGER;
  b: INTEGER;

PROCEDURE Swap(VAR x, y: INTEGER);
  VAR
    temp: INTEGER;
  BEGIN
    temp := x;
    x := y;
    y := temp
END Swap;

BEGIN
	a := 1;
	b := 2;
	Swap(a, b)
END Const.